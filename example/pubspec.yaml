name: basic
description: A simple demonstration of the FQuery package.
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  fquery:
    path: ../
  cupertino_icons: ^1.0.5
  flutter_hooks: ^0.21.2
  dio: ^4.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
